development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  # Action Cable は DB 1。Sidekiq 用 REDIS_URL（DB 2）とは分離する
  url: <%= ENV.fetch("ACTION_CABLE_REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: smart_production

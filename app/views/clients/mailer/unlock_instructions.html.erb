<p><%= t("meetia.mailer.hello", email: @resource.email) %></p>

<p><%= t("meetia.mailer.unlock.intro") %></p>

<p><%= t("meetia.mailer.unlock.link_intro") %></p>

<p><%= link_to t("meetia.mailer.unlock.action"), unlock_url(@resource, unlock_token: @token) %></p>

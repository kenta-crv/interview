# development/test: SQLite3（ローカル開発の手軽さ優先）
# production: PostgreSQL（同時アクセス・データ整合性）

default: &default
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 30000

development:
  <<: *default
  adapter: sqlite3
  database: <%= Rails.root.join('db', 'development.sqlite3').to_s %>
  variables:
    journal_mode: WAL
    synchronous: NORMAL

test:
  <<: *default
  adapter: sqlite3
  database: db/test.sqlite3

# DATABASE_URL があれば優先（Render / Heroku / マネージドDB向け）
# 未設定時は DB_HOST / DB_NAME / DB_USERNAME / DB_PASSWORD を使う
production:
  <<: *default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 15 } %>
  <% if ENV["DATABASE_URL"].present? %>
  url: <%= ENV["DATABASE_URL"] %>
  <% else %>
  host: <%= ENV.fetch("DB_HOST", "localhost") %>
  port: <%= ENV.fetch("DB_PORT", "5432") %>
  database: <%= ENV.fetch("DB_NAME", "meetia_production") %>
  username: <%= ENV.fetch("DB_USERNAME", "meetia") %>
  password: <%= ENV["DB_PASSWORD"] %>
  <% end %>
